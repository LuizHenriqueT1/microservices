server:
  port: 8888
spring:
  application:
    name: erudio-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/LuizHenriqueT1/erudio-config-server.git
          search-paths:
          - 'greeting-service*'
